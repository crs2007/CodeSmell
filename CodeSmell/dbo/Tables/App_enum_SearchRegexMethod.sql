CREATE TABLE [dbo].[App_enum_SearchRegexMethod] (
    [ID]   INT           IDENTITY (1, 1) NOT NULL,
    [Name] VARCHAR (128) NOT NULL,
    CONSTRAINT [PK_App_enum_SearchRegexMethod] PRIMARY KEY CLUSTERED ([ID] ASC)
);

